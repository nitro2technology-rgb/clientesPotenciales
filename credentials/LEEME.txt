Pon aqui el archivo JSON de la Service Account de Google Cloud.

Sugerencia: renombralo a service_account.json y en el .env deja:
    GOOGLE_SERVICE_ACCOUNT_FILE=credentials/service_account.json

Esta carpeta esta ignorada por git (.gitignore), asi que el JSON
nunca se subira a un repositorio.
